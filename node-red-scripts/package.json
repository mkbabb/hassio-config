{
    "name": "node-red-scripts",
    "version": "1.0.1",
    "description": "Collection of scripts for usage within Home Assistant's Node Red instance",
    "type": "module",
    "scripts": {
        "build": "tsx build.ts -i src -o dist -r",
        "watch": "tsx build.ts -i src -o dist -r -w",
        "build:deploy": "tsx build.ts -i src -o dist -r --deploy",
        "deploy": "tsx src/deploy/deploy.ts",
        "deploy:all": "tsx src/deploy/deploy.ts --all",
        "deploy:dry-run": "tsx src/deploy/deploy.ts --dry-run",
        "map": "tsx build.ts --map",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "@rollup/plugin-commonjs": "^28.0.3",
        "@rollup/plugin-node-resolve": "^16.0.1",
        "chalk": "^5.4.1",
        "chokidar": "^4.0.3",
        "dotenv": "^16.3.1",
        "esbuild": "^0.25.3",
        "node-fetch": "^3.3.2",
        "prettier": "^3.5.3",
        "tsx": "^4.19.3",
        "typescript": "^5.8.3",
        "vite": "^6.3.2",
        "vite-plugin-commonjs": "^0.10.4",
        "yargs": "^17.7.2"
    },
    "author": "Mike Babb <mike@babb.dev>",
    "license": "ISC",
    "devDependencies": {
        "@types/node": "^22.14.1",
        "@types/yargs": "^17.0.33"
    }
}
